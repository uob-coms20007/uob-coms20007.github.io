

{: .defn }
A string $w$ is __matched__ by a regex $R$ (over some alphabet $\Sigma$) just if $R \rreds{w} \epsilon$.  The set of all strings that can be matched by $R$ is called the __language of $R$__, and is defined as $$L(R) = \{w \in \Sigma^* \mid R \rreds{w} \epsilon \}$$.